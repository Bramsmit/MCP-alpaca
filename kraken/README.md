# kraken/

**Kraken spot USD** range-bot — zelfde strategie als Alpaca (`alpaca_bot/strategy_core.py`).

- Run: `python -m kraken.live_trader`
- Runtime: `kraken_runtime.py` (ccxt)
- Secrets / Telegram: zie repo-root `.env.example` en `.github/workflows/trade_kraken.yml`.
